# Evidence: es-419 GL suite pins for the D17 two-set schema (sample-burrito primary set)

**Date:** 2026-07-31 · **Method:** live DCS API (`git.door43.org/api/v1`) + sb-zip export
metadata, queried from this workstation. All values below are [VERIFIED — DCS API, 2026-07-31].

## Why

D17/D30 (OPEN-QUESTIONS #28) make `checking/resources.json` pin TWO language sets. The
sample burrito needs a real primary-GL set. Evidence-first: every pin below is a real
repo, a real release tag, and the tag's real commit SHA.

## Pinned set — org `Es-419_gl` (Latin American Spanish GL)

| Repo | Latest release tag | Tag commit SHA | sb-zip export |
|---|---|---|---|
| `es-419_tn` | `v66` (published 2024-07-17) | `22f3d0c61e2ab4701cb869547de9c3c43da07208` | `/sb/v66.zip` → HTTP 200 |
| `es-419_twl` | `v18` | `c2d9547ec95e40c2ba085c597b127c94733feb8a` | `/sb/v18.zip` → HTTP 200 |
| `es-419_tw` | `v37` (published 2024-07-17) | `7586f4ff1f0483ea40a4a68e5e1f33158e08c208` | `/sb/v37.zip` → HTTP 200 |
| `es-419_ta` | `v4` (published 2024-07-17) | `26606b578c37cc2c0ee09bb7b9a291860ff59444` | `/sb/v4.zip` → HTTP 200 |

## Coverage (drives the sample's (tool, book) resolution)

- `es-419_tn` at tag `v66` contains 4 TSV books: `3JN, JON, RUT, TIT`. **TIT is covered.**
- `es-419_twl` at tag `v18` contains 4 TSV books, **incl. TIT and JON**.
- So for the sample project (TIT + JON), both `translationNotes` and `translationWords`
  resolve to the **primary** rung. The fallback rung is exercised by the harness's
  resolution fixture (a book outside the 4-book coverage resolves to English).
- Observation, no action: D17 recorded "es-419_tn covers 14/66 books [VERIFIED via DCS API
  2026-07-12]". At tag `v66` today the count is 4. The 2026-07-12 measurement likely read a
  different ref (e.g. `master`, which for `es-419_twl` holds all 66 books). D17's driver —
  partial coverage — stands either way.

## twl SB flavor

The DCS sb-zip export is the pin target (OPEN-QUESTIONS #24). The export's own metadata
for `unfoldingWord/en_twl` `/sb/v86.zip` says:

- `type.flavorType.name` = `parascriptural`, `flavor.name` = **`x-bcvarticles`**
- `identification.primary.dcs[...].revision` = `570e76d0024c847689e48a20e2ac1a1d2c6eb6e3`
  — equal to the en_twl v86 SHA already recorded in `sb-zip-zaln-2026-07-25.md`.

So twl pin entries carry `"flavor": "parascriptural/x-bcvarticles"` (what the export
declares), and the en_twl fallback pin is `v86` + that SHA (coherent with the
installed-suite era of en_tn v86 / en_tw v87 / en_ta v86).
