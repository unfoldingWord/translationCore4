# The rig job on the GitHub runner: both controls green (2026-09-04)

**Question:** does `.github/workflows/rig.yml` (L-1b of #154) run the rig-gated suites on a
GitHub-hosted runner, and does its negative control refuse the rig suites on the dirtied
rig? The local macOS witness is `rig-job-2026-09-04.md`; this record is the runner.

**Method:** the workflow as committed, on `ubuntu-24.04`, triggered by pull request #168.
Two runs, one per commit of the pull request. Each run is one job: install, build the
client, `setup-from-pins.zsh`, fetch en_ult v89, `seed.zsh`, `run.zsh`, `npm run prove`
with `RIG_REPOS`, the manifest assertion, then the negative control (a second `prove` on
the same rig).

**Run 1 — commit f59414a:** https://github.com/unfoldingWord/translationCore4/actions/runs/33919640738.
Job time 3 min 7 s. The server build took 7.5 s: the cargo cache key is the one
`package-desktop.yml` uses, and it hit. The en_ult fetch ran (cache miss on the first run)
and matched the pinned revision. The rig answered `pkg_version 0.18.5` on `linux`.

```
PASS  conformance:transport  10 passed  (1.1s)
PASS  conformance:roundtrip  12 passed  (1.5s)
PASS  vitest:rig             41 passed  (1.4s)
PROVE OK
```

Negative control, same job: `rig suites: REFUSED: rig not pristine — extra repos under
_local_/_local_: ...`; the manifest assertion printed `negative control: every rig suite
skipped with REFUSED`. This run's negative control accepted any `REFUSED` reason and
uploaded whatever manifest was on disk; 8704b3a tightened both.

**Run 2 — commit 8704b3a (the workflow as merged):** https://github.com/unfoldingWord/translationCore4/actions/runs/33921387286.
Job time 2 min 53 s. Server build 6.75 s (cache hit). The same three suites: 10, 12, 41
passed, `PROVE OK`. Negative control: `negative control: every rig suite skipped with
REFUSED: rig not pristine ...`, after the checked-out manifest was removed before each
`prove`, so the file read was this run's. Artifacts: `prove-manifest-rig`,
`prove-manifest-rig-negative-control`.

The committed `docs/evidence/manifest.json` stays the clean-clone manifest from the
push-event `verify` run; the rig job's manifest is the artifact.

[VERIFIED — GitHub Actions runs 33919640738 (f59414a) and 33921387286 (8704b3a),
ubuntu-24.04, 2026-09-04; pankosmia-web 0.18.5 (99fd9be)]

**Not measured:** a cold cargo build on the runner. The cache hit on both runs, so the
time of the first build after a server pin change is unknown. The negative control ran
after a green positive run only; the workflow skips it when the positive run fails, since
a rig the suites never touched is not the dirty rig the control is about.
