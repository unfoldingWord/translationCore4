# fixtures/vrs — vendored versification schemes

`eng.json` (27,681 bytes) is a byte-for-byte copy of the Pankosmia platform's shipped
scheme `templates/content_templates/vrs/eng.json` (the assembled Pankosmia app-resources;
the server also serves it live at `GET /content-utils/versification/eng`). Vendored
2026-07-30 for the conformance harness. The platform writes this scheme into each created
project as `ingredients/vrs.json` (`new_text_translation.rs` — [VERIFIED — pankosmia-web
0.18.5 (99fd9be, 2026-07-30)]; the vrs files are unchanged 0.16.18→0.18.5).

Do not edit this file. To refresh it, copy it from the platform again and record the new
date here.
