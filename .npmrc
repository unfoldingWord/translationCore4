; REQUIRED (PLATFORM-NOTES #3): word-aligner@1.0.3 declares a usfm-js ^2 peer
; while the proven production pairing is usfm-js 3.4.3. The pairing is proven
; behaviorally by the conformance harness (34 checks) — do not "fix" the peer conflict by
; changing versions (TEST-PLAN §1 dependency-audit honesty note).
legacy-peer-deps=true
